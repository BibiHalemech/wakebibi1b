#!/bin/sh
# Drop agent trailers so only BibiHalemech remains in the commit message.
tmp="${1}.stripped"
sed -e '/^Co-authored-by: Cursor /d' \
    -e '/^Co-authored-by:.*cursoragent@/d' \
    "$1" > "$tmp"
mv "$tmp" "$1"
